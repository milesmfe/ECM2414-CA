package cards;

/**
 * GameStatus enum. Defines each status that CardGame
 * can have.
 * 
 * @author Miles Edwards
 * @version 1.0
 * 
 */
public enum GameStatus {
    NOT_SETUP,
    SETUP_IDLE,
    SETUP_ACTIVE,
    GAME_WON;
}
